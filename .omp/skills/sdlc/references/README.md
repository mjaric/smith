# SDLC loop — references

Learned context from past rounds and retrospectives. The loop skill reads
relevant files here when making decisions (e.g. "why we chose X", "known
flaky test Y", "reviewer kept flagging Z").

`/forge retrospect` proposes new reference files here; you approve them.
